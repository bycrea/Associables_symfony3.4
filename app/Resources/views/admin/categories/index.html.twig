{% extends 'base.html.twig' %}

{% block content %}

    <h1>Liste des categories admin</h1>

    {% for category in categories %}
        <span>{{ category.name }}</span>
        <a href="{{ path('admin_categories_update', {id: category.id}) }}">Editer</a>
        <a href="{{ path('admin_categories_delete', {id: category.id}) }}">Supprimer</a>
        <br>
    {% endfor %}

    <a href="{{ path('admin_categories_create') }}">Ajouter</a>

{% endblock %}
